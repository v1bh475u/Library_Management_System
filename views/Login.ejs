<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login and Signup</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }

    body {
      background: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .form-container {
      padding: 3rem 6rem;
      background: black;
      border-radius: .5rem;
      box-shadow: 0 0 1rem rgba(0, 0, 0, 0.1);
      width: auto;
      max-width: 100%;
      overflow: hidden;
      position: relative;
      margin: 1rem;
    }

    form {
      width: auto;
    }

    form.active {
      display: block;
    }

    h2 {
      margin-bottom: 1rem;
      color: #333;
    }

    .input-group {
      margin-bottom: 1rem;
    }

    .input-group label {
      margin-bottom: 5px;
      display: block;
      color: #666;
    }

    .input-group input {
      width: 100%;
      padding: .5rem;
      border: none;
      border-radius: 0.5rem;
      font-size: 16px;
      outline: none;
    }

    button {
      width: 100%;
      padding: .5rem;
      border: none;
      border-radius: 5px;
      background: #5cb85c;
      color: black;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background: #4cae4c;
    }

    .message {
      margin-top: 15px;
      font-size: 14px;
      color: #666;
      text-align: center;
    }

    .message a {
      color: #5cb85c;
      text-decoration: none;
    }

    .message a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="form-container">
      <form id="loginForm" method="post" action="/login">
        <h2>Login</h2>
        <div class="input-group">
          <input type="username" id="username" name="username" required placeholder="Username">
        </div>
        <div class="input-group">
          <input type="password" id="password" name="password" required placeholder="Password">
        </div>
        <button type="submit">Login</button>
        <p class="message">Not registered? <a href="/register" id="showSignup">Create an account</a></p>
      </form>
    </div>

  </div>

</body>

</html>